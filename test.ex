defmodule M do
  def main do
    # name = IO.gets("What is your name? ") |> String.trim()
    # IO.puts("Hello #{name}")
    data_stuff()
  end

  def data_stuff do
    # my_int = 16
    # IO.puts("Integer #{is_integer(my_int)}")

    # my_float = 2.13
    # IO.puts("Float #{is_float(my_float)}")

    # IO.puts("Atom #{is_atom(:harry)}")

    # ranges from 1 to 10
    # one_to_ten = 1..10
  end
end
